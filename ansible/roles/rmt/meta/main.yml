---
galaxy_info:
  author: SUSE RMT Team
  description: Ansible role to install and configure SUSE RMT (Repository Mirroring Tool) server
  company: SUSE
  license: MIT
  # Requires ansible-core 2.18 (provided by Ansible >= 11 in SLE 16.x)
  min_ansible_version: "2.18"

  platforms:
    # leap 16+ is not available
    # - name: opensuse
    #   versions:
    #     - Leap16
    - name: SLES
      versions:
        - "16"

  galaxy_tags:
    - suse
    - rmt
    - repository
    - mirroring
    - scc

dependencies: []
